@{
    @model StudentScheduleVM
    @inject IRepositoryManager repository

    ViewData["Title"] = "Add StudentSchedule";

    var GroupList = new List<Group>();
    var res = await repository.GroupRepository.GetAllGroupsInCurrentYear();

    if (res.Count > 0)
    {
        GroupList = res.ToList();

    }

    var YearList = new List<Year>();
    var yearRes = await repository.YearRepository.GetAll();
    if(yearRes.Count > 0)
    {
        YearList = yearRes.ToList();
    }

    var TermList = new List<Term>();
    var termRes = await repository.TermRepository.GetAll();
    if(termRes.Count > 0)
    {
        TermList = termRes.ToList();
    }

}



<!--Start Main content container-->
<div class="main_content_container">
    <div class="main_container  main_menu_open">
        <!--Start system bath-->
        <div class="home_pass hidden-xs">
            <ul>
                <li class="bring_right"><span class="glyphicon glyphicon-home "></span></li>
                 <li class="bring_right"><a asp-action="index">الجداول الدراسية</a></li>
                <li class="bring_right"><a asp-action="StudentSchedule">جداول الطلاب</a></li>
                <li class="bring_right"><a href="">اضافة جدول جديد </a></li>
            </ul>
        </div>
        <!--/End system bath-->
        <div class="page_content">

            <h1 class="heading_title">اضافة جدول جديد</h1>


            <!--Start status alert-->
            @if (TempData.ContainsKey("error"))
            {

                <div class="alert alert-danger h4" role="alert">@TempData["error"]</div>
            }

            @if (TempData.ContainsKey("msg"))
            {
                <div class="alert alert-success h5" role="alert">@TempData["msg"]</div>
            }
            <!--/End status alert-->


            <div class="form">
                <form class="form-horizontal" method="post" enctype="multipart/form-data" asp-action="CreateStudentSchedule">
                    <div asp-validation-summary="All" class=" alert-danger"></div>

                    <div class="form-group">
                        <label  class="col-sm-2 control-label bring_right left_text">الجدول</label>
                        <div class="col-sm-10">
                            <input type="file" required name="file"  class="form-control" placeholder="الجدول">
                            <span class="text-danger" ></span>
                        </div>
                    </div>
                     <div class="form-group">
                        <label asp-for="YearId" class="col-sm-2 control-label bring_right left_text">السنة الدراسية</label>
                        <div class="col-sm-10">
                            <select id="input3" asp-for="YearId" class="form-control" asp-items='@new SelectList(YearList,"YearId","Year1")'>
                                <option value="@null">اختر سنة دراسية...</option>
                            </select>

                            <span class="text-danger" asp-validation-for="YearId"></span>
                        </div>
                    </div>
                     <div class="form-group">
                        <label asp-for="YearId" class="col-sm-2 control-label bring_right left_text">الرتم الدراسي</label>
                         <div class="col-sm-10">
                        <select  asp-for="TermId" class="form-control" asp-items='@new SelectList(TermList,"TermId","Name")'>
                            <option value="@null">اختر ترم دراسي...</option>
                        </select>

                        <span class="text-danger" asp-validation-for="TermId"></span>
                    </div>
                    </div>
                    
                    <div class="form-group">
                        <label asp-for="GroupId" class="col-sm-2 control-label bring_right left_text">المجموعة</label>
                        <div class="col-sm-10">
                            <select asp-for="GroupId" class="form-control" asp-items='@new SelectList(GroupList,"GroupId","Name")'>
                                <option value="@null">اختر مجموعة...</option>
                            </select>
                            <span class="text-danger" asp-validation-for="GroupId"></span>
                        </div>
                    </div>
                    <div class="form-group">
                        <div class="col-sm-12 text-center">
                            <button type="submit"  class="btn btn-primary">اضافة </button>

                        </div>
                    </div>


                </form>
            </div>
        </div>
    </div>
</div>


<!--/End Main content container-->