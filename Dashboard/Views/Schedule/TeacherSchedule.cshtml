@{
    ViewData["Title"] = "TeacherSchedule Page";
    @model SearchTeacherSchedule ;
    @inject IRepositoryManager repository

    List<SearchTeacherSchedule> data = (List<SearchTeacherSchedule>)ViewData["Data"];

    var TeacherList = new List<Teacher>();
    var teacherRes = await repository.TeacherRepository.GetAll();

    if (teacherRes.Count > 0)
    {
        TeacherList = teacherRes.ToList();
    }
    var YearList = new List<Year>();
    var yearRes = await repository.YearRepository.GetAll();
    if(yearRes.Count > 0)
    {
        YearList = yearRes.ToList();
    }

    var TermList = new List<Term>();
    var termRes = await repository.TermRepository.GetAll();
    if(termRes.Count > 0)
    {
        TermList = termRes.ToList();
    }

    int TeacherScheduleCount;
    if (data == null)
    {
        TeacherScheduleCount = 0;
    }
    else
    {
        TeacherScheduleCount = data.Count();
    }



}
<!--Start Main content container-->
<div class="main_content_container">
    <div class="main_container  main_menu_open">
        <!--Start system bath-->
        <div class="home_pass hidden-xs">
            <ul>
                <li class="bring_right"><span class="glyphicon glyphicon-home "></span></li>
                <li class="bring_right"><a asp-action="index">الجداول الدراسية</a></li>
                <li class="bring_right"><a asp-action="TeacherSchedule">جداول المدرسين</a></li>
            </ul>
        </div>
        <!--/End system bath-->
        <div class="page_content">

            <h1 class="heading_title">البحث عن الجدول <span class="fa fa-search " style="margin: 5px;"></span></h1>


            <!--Start status alert-->
            <!--/End status alert-->

            <div class="form">
                <form method="post" class="form-horizontal" asp-action="TeacherSchedule">

                    <div class="form-group">
                        <label asp-for="TeacherId" class="col-sm-2 control-label bring_right left_text">المدرس</label>
                        <div class="col-sm-10">
                            <select id="input0" asp-for="TeacherId" class="form-control" asp-items='@new SelectList(TeacherList,"TeacherId","Name")'>
                                <option value="@null">اختر مدرس...</option>
                            </select>

                            <span class="text-danger" asp-validation-for="TeacherId"></span>
                        </div>
                    </div>
                    <div id="additionalFilters" style="display:none;">
                        <div class="form-group">
                            <label asp-for="ScheduleId" class="col-sm-2 control-label bring_right left_text">رقم الجدول</label>
                            <div class="col-sm-10">
                                <input asp-for="ScheduleId" type="text" class="form-control" id="input2" placeholder="البحث برقم الجدول">
                            </div>
                        </div>
                        <div class="form-group">
                            <label asp-for="YearId" class="col-sm-2 control-label bring_right left_text">السنة الدراسية</label>
                            <div class="col-sm-10">
                                <select id="input3" asp-for="YearId" class="form-control" asp-items='@new SelectList(YearList,"YearId","Year1")'>
                                    <option value="@null">اختر سنة دراسية...</option>
                                </select>

                                <span class="text-danger" asp-validation-for="YearId"></span>
                            </div>
                        </div>
                    </div>

                    <div class="form-group">
                        <div class="col-sm-12 text-center">
                            <button type="submit" class="btn btn-primary"> البحث</button>
                            <button type="button" class="btn btn-default" onclick="showFilters()"> <span class="fa fa-filter"></span></button>
                        </div>
                    </div>
                </form>
            </div>
            <a class="bring_left add-new" asp-action="CreateTeacherSchedule">اضافة جدول جديد <span style="padding-right: 8px;" class="fa fa-user-plus"></span></a>
            <br />
            <br />
            @if (TeacherScheduleCount > 0)
            {
                <div class="wrap">
                    <table class="table table-bordered">
                        <tr>
                            <td>#</td>
                            <td>رقم الجدول</td>
                            <td>تاريخ التحميل</td>
                            <td>السنة الدراسية</td>
                            <td>المدرس</td>
                            <td>الترم</td>
                            <td>التحكم</td>
                        </tr>

                        @{
                            var count = 1;
                            foreach (var i in data)
                            {
                                var TeacherName = TeacherList.FirstOrDefault(g => g.TeacherId == i.TeacherId)?.Name ?? "غير معروف";
                                var year = YearList.FirstOrDefault(g => g.YearId == i.YearId)?.Year1 ;
                                var term = TermList.FirstOrDefault(g => g.TermId == i.TermId)?.Name ;
                                <tr>
                                    <td>@count</td>
                                    <td>@i.ScheduleId</td>
                                    <td>@i.DateTime?.ToString("yyyy/MM/dd")</td>
                                    <td>@year</td>
                                    <td>@TeacherName</td>
                                    <td>@term</td>
                                    <td>
                                        <a asp-action="EditTeacherSchedule" asp-route-Id="@i.ScheduleId" class="btn alert-info">تعديل <i class="fa fa-fw fa-edit"></i></a>
                                        <a asp-action="DownloadTeacherSchedule" asp-route-Id="@i.ScheduleId" class="btn alert-success"><b>تنزيل </b><i class="fa fa-download"> </i></a>
                                    </td>
                                </tr>
                                count++;
                            }
                        }

                    </table>
                </div>
            }

            <!--Start status alert-->
            @if (TempData.ContainsKey("error"))
            {

                <div class="alert alert-danger h4" role="alert">@TempData["error"]</div>
            }

            @if (TempData.ContainsKey("msg"))
            {
                <div class="alert alert-success h5" role="alert">@TempData["msg"]</div>
            }
            <!--/End status alert-->


        </div>
    </div>
</div>

<!--/End Main content container-->

<script>
                function showFilters() {
        var filters = document.getElementById('additionalFilters');
        if (filters.style.display === 'none') {
            filters.style.display = 'block';
        } else {
            // إخفاء الفورمات
            filters.style.display = 'none';
            // حذف البيانات من الفورمات
            document.getElementById('input2').value = '';
            document.getElementById('input3').value = '';
            document.getElementById('input4').value = '';
        }
    }

</script>